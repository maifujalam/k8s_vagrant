1. k -n two get secrets s3 -o jsonpath="{.data.*}" | base64 -d
2. search: distribute credentials securely
3. Important: Make sure the mount path of the secret is verified after solving question in exam.Use cat /mount/path to
   by exec into container to validate secret.
4.