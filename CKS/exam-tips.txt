1. Vis: v.Then select with arrow.
    > right shift
    < left shift
2. select + right clist to paste.
3. After launch of the exam in terminal.
    vi ~/.vimrc
    set et
    set ts=2
    set sw=2
4. Create macros
    export do="--dry-run=client -o yaml"
    export now="--force --grace-period 0"

    Whenever required,use with $do ,$now.
5.  Crictl:
   Get container id: crictl ps -id=<container_id>
   Get pod details: crictl pod -id=<pod_id>

6. Debug kube api server: journalctl -fu kubelet | grep apiserver
7. k get deployments.apps  -o custom-columns=NAME:.metadata.name,IMAGE:.spec.template.spec.containers[*].image -A
8. For falco do not update the default rules file falco.rules.yaml rather make changes in falco.local.rule.yaml.
9. When ever disabling automountServiceAccountToken: false.Make sure the token is also not projected and used as volume.
10. To restart api server,mv kubernetes-kubeapiser.yaml to temp and then again move this to /etc/kubernetes/manifest/.
11. https://itnext.io/cks-exam-series-10-container-hardening-177588b8bbfe